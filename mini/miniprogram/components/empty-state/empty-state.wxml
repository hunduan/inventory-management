<view class="empty-state">
  <text class="empty-icon">{{icon}}</text>
  <text class="empty-text">{{text}}</text>
  <button wx:if="{{actionText}}" class="btn btn-primary btn-sm" bindtap="onAction">{{actionText}}</button>
</view>

<view class="empty-state">
  <text class="empty-icon">{{icon}}</text>
  <text class="empty-text">{{text}}</text>
  <button wx:if="{{showButton}}" class="btn-primary empty-btn" bindtap="onTapButton">{{buttonText}}</button>
</view>
